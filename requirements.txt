# CrisisLens Python Dependencies
# Note: PySpark, Delta Lake, and MLlib are PRE-INSTALLED on all Databricks clusters
# Only add libraries NOT already available in the Databricks Runtime

# Geographic visualization
folium==0.14.0         # Interactive Leaflet.js maps from Python
branca==0.6.0          # Color maps and HTML utilities for folium

# Standard data science stack (already on Databricks, listed for local dev)
# pandas>=1.5.0
# numpy>=1.23.0
# matplotlib>=3.6.0
# scikit-learn>=1.1.0
# requests>=2.28.0

# If running locally (not on Databricks), you also need:
# pyspark==3.4.0
# delta-spark==2.4.0
